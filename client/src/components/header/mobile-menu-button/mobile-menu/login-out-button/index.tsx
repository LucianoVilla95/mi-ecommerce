import { JSX } from 'react';

const LoginOutButton = (): JSX.Element => {
  return (
    <button className="mt-auto bg-neutral-100 rounded-2xl p-4">
      Cerrar Sesión
    </button>
  )
};

export default LoginOutButton;